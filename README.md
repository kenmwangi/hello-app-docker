## Testing Django apps using Docker

New concepts that learnt so far:
1. Writing **Dockerfile** and building initial **image**
1. Writing **docker-compose.yml** and running the container with **docker-compose up** command